(ns hs-deck-helper-cljs-test-front.ipc
  (:require  [cljs.test :refer-macros [async deftest is testing]]
             [common.logger :as logger]
             [hs-deck-helper-cljs-front.ipc :as ipc]))


(deftest test-test
  (is (= 1 1)))
